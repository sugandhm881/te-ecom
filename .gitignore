node_modules/
.env
# Hand-made .env backups taken before a risky edit. Plaintext secrets — never in git.
.env.bak*
# Encrypted secrets vault + master key. The vault is ciphertext, but it is per-environment and
# never belongs in git; the key must never be near the repo at all.
.env.vault
.env.vault.tmp-*
.master.key
*.master.key
master_order_data.json
master_api_log.json
.DS_Store
awb_assignment_cache.json
rapidshyp_cache.json
package.json
package-lock.json
service_account.json
COD_Confirmation.json
order_shipment_cache.json
amazon_cache_date.txt
# data-sync incremental cursor (runtime state, do not commit)
.sync_state.json

# Stray local zips (browser-extension bundles, backups) — never commit
*.zip

# Tally bridge agent secrets + its rotating log (runtime, never commit)
tally-bridge/.env
tally-bridge/.env.vault
tally-bridge/agent.log*

# Bank statements — real financial data, never commit
*Account Statement*.xlsx
*Account Statement*.xls
*Account Statement*.csv
tests/out/
